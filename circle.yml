machine:
  java:
    version: oraclejdk8
database:
  post:
    - mysql -u root --execute "create database if not exists sample character set utf8"
dependencies:
  override:
    - mvn dependency:resolve
test:
  pre:
    - mvn flyway:info
  override:
    - mvn flyway:migrate
  post:
    - mvn flyway:info
deployment:
  deploy:
    branch: deploy_page
    commands:
      - mvn schemaspy:schemaspy
      - git config --global user.email higedrum.coz@gmail.com
      - git config --global user.name su-kun1899
      - git add -A
      - git commit ./docs/* -m "Update schemaSpy html [ci skip]"
      - git push git@github.com:su-kun1899/schemaspy-plugin-sample.git $CIRCLE_BRANCH
      - .circleci/deploy.sh